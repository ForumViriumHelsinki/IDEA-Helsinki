apiVersion: apps/v1
kind: Deployment
metadata:
  name: fcd-manager
  namespace: idea-helsinki
  labels:
    app.kubernetes.io/name: fcd-manager
    app.kubernetes.io/component: data-sync
    app.kubernetes.io/part-of: idea-helsinki
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: fcd-manager
      app.kubernetes.io/component: data-sync
  template:
    metadata:
      labels:
        app.kubernetes.io/name: fcd-manager
        app.kubernetes.io/component: data-sync
        app.kubernetes.io/part-of: idea-helsinki
    spec:
      containers:
      - name: fcd-manager
        image: fcd-manager
        ports:
        - name: http
          containerPort: 8080
        envFrom:
        - secretRef:
            name: idea-helsinki-secrets
        env:
        - name: STORAGE_EMULATOR_HOST
          value: "http://fake-gcs-server:4443"
        - name: GCS_BUCKET_NAME
          value: "idea-helsinki-dev"
        - name: GCS_PREFIX
          value: "idea-helsinki/"
        - name: FEATURE_FLAG_USE_SQLITE_STORAGE
          value: "true"
        volumeMounts:
        - name: data-volume
          mountPath: /app/data
        - name: sqlite-data
          mountPath: /app/sqlite
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /healthz
            port: http
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        startupProbe:
          httpGet:
            path: /startup
            port: http
          initialDelaySeconds: 0
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 30
      volumes:
      - name: data-volume
        hostPath:
          path: /Users/lgates/repos/IDEA-Helsinki/data
          type: Directory
      - name: sqlite-data
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: fcd-manager
  namespace: idea-helsinki
  labels:
    app.kubernetes.io/name: fcd-manager
    app.kubernetes.io/component: data-sync
    app.kubernetes.io/part-of: idea-helsinki
spec:
  selector:
    app.kubernetes.io/name: fcd-manager
    app.kubernetes.io/component: data-sync
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  type: ClusterIP
